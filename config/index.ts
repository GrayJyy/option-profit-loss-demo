import { ChartDimensions } from '@/types'

export const defaultDimensions: ChartDimensions = {
  width: 600,
  height: 400,
  margin: {
    top: 80,
    right: 40,
    bottom: 60,
    left: 40,
  },
}
